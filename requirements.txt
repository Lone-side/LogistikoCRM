# ============================================================================
# LogistikoCRM - Production Requirements
# Python Dependencies για Λογιστικό Γραφείο
# ============================================================================

# Django Core
Django>=5.2,<5.3  # 5.2 LTS — το 5.0 είναι EOL χωρίς security fixes
django-cors-headers>=4.3.0
django-environ>=0.11.2
polib>=1.2.0  # Translation file handling (.po files)

# Database
psycopg2-binary>=2.9.9  # PostgreSQL adapter (production)
# mysqlclient>=2.2.0  # MySQL adapter (προαιρετικό - θέλει MySQL headers:
#                       sudo apt install default-libmysqlclient-dev build-essential
#                       Τοπικά ΔΕΝ χρειάζεται (default: SQLite)

# REST API
djangorestframework>=3.14.0
djangorestframework-simplejwt>=5.3.0
django-filter>=23.5  # Filtering for DRF ViewSets
drf-spectacular>=0.27.0  # OpenAPI schema generation

# Async Tasks & Queue
celery>=5.3.4
redis>=5.0.1  # Celery broker
django-redis>=5.4.0  # Cache/session backend όταν έχει οριστεί REDIS_CACHE_URL
django-q2>=1.6.0  # Django 5.0+ compatible (fork of django-q)
tendo>=0.3.0  # Singleton instance control

# File Processing
openpyxl>=3.1.2  # Excel export/import
Pillow>=10.1.0  # Image processing
python-magic>=0.4.27  # File type validation (SECURITY)
# ⚠️ ΚΑΡΦΩΜΕΝΕΣ ΕΚΔΟΣΕΙΣ — ΜΗΝ ΤΙΣ ΧΑΛΑΡΩΣΕΙΣ ΧΩΡΙΣ ΕΛΕΓΧΟ ΕΛΛΗΝΙΚΩΝ.
# Το pypdf 6.15.0 εξάγει το ελληνικό «Δ» (U+0394) ως «∆» (U+2206, INCREMENT),
# οπότε κάθε αναζήτηση σε έγγραφο με Δ (ΔΗΛΩΣΗ, ΔΟΥ, ΑΔΕΙΑ…) γυρίζει κενή.
# Το 6.14.2 είναι σωστό. Regression test: tests/accounting/test_pdf_greek_extraction.py
pypdf==6.14.2  # Εξαγωγή κειμένου από ψηφιακά PDF (αρχειοθέτηση/αναζήτηση)
xhtml2pdf==0.2.17  # PDF report generation (Windows compatible)
XlsxWriter>=3.2.0  # Excel writing
pandas>=2.0.0  # Data analysis
numpy>=1.24.0  # Numerical computing

# Email & Communication
requests>=2.31.0  # HTTP requests (door control, APIs)
aiohttp>=3.9.0  # Async HTTP client/server

# GeoIP (Optional - για location tracking)
geoip2>=4.7.0

# Security & Environment
python-dotenv>=1.0.0  # .env file support
cryptography>=50.0.0  # Fernet (mydata/encryption.py) — ελάχιστη ασφαλής έκδοση (PYSEC-2026-3552, pip-audit 08/2026)
# Ελάχιστες ασφαλείς εκδόσεις transitive dependencies (pip-audit 07/2026)
PyJWT>=2.13.0  # χρησιμοποιείται από djangorestframework-simplejwt
urllib3>=2.7.0
idna>=3.15

# Development & Testing (Optional)
# Uncomment για development environment:
# django-debug-toolbar>=4.2.0
# pytest>=7.4.3
# pytest-django>=4.7.0
# coverage>=7.3.2
# factory-boy>=3.3.0
# faker>=20.1.0

# ============================================================================
# Production Server (WSGI)
# ============================================================================

# Gunicorn (Recommended για production)
gunicorn>=21.2.0

# Alternative: uWSGI
# uwsgi>=2.0.23

# ============================================================================
# Additional Production Tools
# ============================================================================

# Monitoring & Logging
sentry-sdk>=1.39.1  # Error tracking (optional)

# Static files
whitenoise>=6.6.0  # Static file serving

# Admin enhancements
django-import-export>=3.3.5  # Better import/export (optional)

# ============================================================================
# Notes:
# - Install: pip install -r requirements.txt
# - Για PostgreSQL: apt-get install libpq-dev python3-dev
# - Για python-magic: apt-get install libmagic1
# - Για production: pip freeze > requirements-lock.txt για version pinning
# ============================================================================
